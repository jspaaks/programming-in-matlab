% Main program showing two applications of conv_deg2rad.

% Direction in degrees
  DirDeg = 45;

% Slope in degrees
  SlopeDeg = 12; 

% Use function m-file conv_deg2rad to convert unit
% of direction to radians:
  [DirRad] = conv_deg2rad(DirDeg);

% Use function m-file conv_deg2rad to convert unit 
% of slope to radians:
  [SlopeRad] = conv_deg2rad(SlopeDeg);

% End of program
